---
"compass": minor
---

Vault entries can now be edited inline without deleting and re-adding them. Clicking the pencil icon on any entry flips it into an edit form pre-filled with current values; sensitive fields default to hidden with a reveal toggle; pressing Esc or Cancel discards changes; Save commits the update (encrypted) and snapshots the previous values in history.
